# 猫家日常四大功能优化登记

更新时间：2026-08-15

本登记覆盖猫家点菜、猫家记事本、猫咪清单、推荐清单四个一级入口。所有记录、护理、推荐、打卡均保存在本机 SQLite；图片使用项目内静态文件，避免依赖外链加载。

## 1. 猫家点菜

### 已完成的 10 项技术与 UI 优化

| # | 优化项 | 落地方式 |
|---|---|---|
| 1 | 四步导航隔离 | 仅点菜流显示步骤条，其他入口不再误出现 1-4 导航。 |
| 2 | 日期切换 | 餐别页支持前一天、今天、后一天，切换后刷新同日期数据。 |
| 3 | 用餐状态面板 | 展示日期、餐别、已选人数和当前完成状态。 |
| 4 | 同餐去重 | 支持本周不重复，降低连续点相同菜的概率。 |
| 5 | 智能排序 | 推荐、常点、尝鲜三种排序可切换。 |
| 6 | 图片性能 | 菜品使用固定尺寸、懒加载、异步解码与本地图片路径校验。 |
| 7 | 筛选可达性 | 分类、收藏、搜索均带语义状态和可见焦点。 |
| 8 | 选择备注 | 少辣等快速备注与手动备注永久保存在本餐选择中。 |
| 9 | 完成反馈 | 选菜后有实时家庭动态、完成页状态和尊重减弱动态设置的庆祝动画。 |
| 10 | 连接韧性 | 请求超时处理、SSE 重连和在线状态提示，减少手机弱网困惑。 |

### 已完成的 5 项功能

1. 餐别日期浏览：可以直接查看过去或未来某天的中饭、晚饭安排。
2. 本餐完成度：在选菜前即可看见还有几位家人未选。
3. 食谱厨房模式：份量、食材勾选、步骤进度、计时器与购物清单复制。
4. 摇一摇推荐：从可选菜里随机挑选，并避开当前选择。
5. 家庭协作：历史复用、投票选菜、提醒家人和周菜单入口。

## 2. 猫家记事本

### 已完成的 10 项技术与 UI 优化

| # | 优化项 | 落地方式 |
|---|---|---|
| 1 | 日期索引 | 数据库按日期建立索引，日切换查询稳定。 |
| 2 | 固定排序 | 新增 `pinned` 字段，固定内容始终位于当天顶部。 |
| 3 | 权限收束 | 只有原记录人可以删除或固定自己的记录。 |
| 4 | 草稿保护 | 编辑内容、日期、记录人和 @ 对象自动保留在本机。 |
| 5 | 周览摘要 | 显示近七天数量、固定数和主要记录人。 |
| 6 | 即时同步 | 新增、删除、固定都会通过 SSE 更新其他已打开页面。 |
| 7 | 关键词检索 | 按内容、记录人和 @ 对象筛选当天记录。 |
| 8 | 清晰层级 | 固定项、作者、时间、@ 提醒和操作控件有明确视觉区分。 |
| 9 | 键盘可用 | 可聚焦按钮、可读标签、弹窗焦点回归与实时状态播报。 |
| 10 | 数据可携 | 使用 Markdown 导出当天记录，不锁定在页面里。 |

### 已完成的 5 项功能

1. 固定重要事项：固定/取消固定，且固定项可一眼识别。
2. 近七天摘要：当前日期为终点的七日活动概览。
3. 当天 Markdown 导出：包含作者、时间、@ 提醒与内容。
4. 快捷标签：提醒、采购、外出、重要可快速插入。
5. @ 家人提醒：选择对象后记录保留 @ 标签，并产生家庭动态通知。

## 3. 猫咪清单

### 已完成的 10 项技术与 UI 优化

| # | 优化项 | 落地方式 |
|---|---|---|
| 1 | 独立一级入口 | 宠物清单从点菜流程移出，与其余三项功能平级。 |
| 2 | 真猫头像 | 妹妹、giao、咩咩使用项目内真实照片，避免外链失效。 |
| 3 | 性别标注 | 三只猫的公母信息在卡片、记录与档案里一致显示。 |
| 4 | 周期计算 | 疫苗、内外驱、洗澡、剪指甲、体检自动计算建议时间。 |
| 5 | 状态筛选 | 全部、需关注、近期安排可快速定位待办。 |
| 6 | 项目筛选 | 历史可按每个护理项目过滤，减少长列表扫描。 |
| 7 | 永久记录 | 每次护理写入 SQLite，保留日期、备注与记录人。 |
| 8 | 详情降噪 | 主页卡片只展示关键状态，完整时间线放入档案弹窗。 |
| 9 | 移动端表单 | 日期、项目和备注以大尺寸触控控件排列，单列不拥挤。 |
| 10 | 可读反馈 | 保存、加载、空状态与数据数量都有明确文本反馈。 |

### 已完成的 5 项功能

1. 猫咪档案：点卡片可查看个人资料、建议行动和最近五条时间线。
2. 近期安排：独立筛出未来 14 天内需要安排的护理。
3. 护理项目筛选：一键只看疫苗、驱虫、洗澡、体重等项目。
4. 体重记录统计：每只猫档案显示累计体重记录次数。
5. 档案直达登记：在猫咪档案中直接跳转到对应猫咪的护理登记。

## 4. 推荐清单

### 已完成的 10 项技术与 UI 优化

| # | 优化项 | 落地方式 |
|---|---|---|
| 1 | 本地城市图册 | 19 座城市、57 张图片保存为本地静态资源，首屏不依赖第三方图床。 |
| 2 | 三图预览 | 城市推荐卡每项固定三张图和统一裁切比例，避免图片错位。 |
| 3 | 完整图册 | 点击城市预览可查看带说明的大图册。 |
| 4 | 类型筛选 | 地点、商家、商品和全部可快速筛选。 |
| 5 | 地区筛选 | 自动从真实记录生成地区筛选，不维护重复选项。 |
| 6 | 地图降级 | 无目标时显示默认地图；有坐标时安全生成 OpenStreetMap 嵌入链接。 |
| 7 | 坐标校验 | 服务端校验经纬度范围，并拒绝只填其中一个坐标的错误数据。 |
| 8 | 图片延迟加载 | 图册图片保留固定比例、懒加载和异步解码。 |
| 9 | 打卡统计 | 显示城市、定位数与已打卡地区数，信息随数据刷新。 |
| 10 | 数据关联 | 推荐、图片、打卡和记录人用外键关联，删除/保留规则明确。 |

### 已完成的 5 项功能

1. 城市图册弹窗：点击卡片图片查看完整三图和城市介绍。
2. 自动地区筛选：按国家/地区快速缩小推荐范围。
3. 地图聚焦：从推荐卡或打卡历史直接定位到地图。
4. 当前位置打卡：在手机授权后自动填入坐标，地区和地址仍可手动编辑。
5. 旅行足迹导入：保留历史时间、地点、城市介绍和对应本地图片；泰国仅包含曼谷与芭提雅。

## 5. 2026-08-16：五模块快捷工作流与状态透镜

本轮将点菜、记事本、宠物清单、推荐清单、家庭投票统一为五个平级入口；每个入口都增加“先看状态、再做一件事”的快捷工作流。数据继续使用本地 SQLite 保存，不新增外部账号或付费依赖。

### 本轮已交付的 25 个快捷工作流

| 模块 | 快捷工作流 |
|---|---|
| 猫家点菜 | 明日计划、午晚餐切换、摇一摇推荐、自动购物清单、菜品投票 |
| 猫家记事本 | 新建待办、采购记录预填、提醒全部成员、高优先级筛选、21 日议程 |
| 宠物清单 | 疫苗、体内驱虫、体外驱虫、洗澡、体重登记预填 |
| 推荐清单 | 想去、去过、五星推荐、旅行地点、当前位置打卡 |
| 家庭投票 | 晚餐、外出、家务、采购、约期五种一键模板 |

### 本轮已交付的 25 个状态与技术优化

| 模块 | 状态透镜与实现 |
|---|---|
| 猫家点菜 | 展示当前日期和餐别、选人进度、忌口偏好、重复菜规避和一键重置筛选；选菜结果可直接汇总食材。 |
| 猫家记事本 | 展示今日、未完成、高优先级、置顶和近期议程；日期有事项可被快速识别，待办和截止日期维持可见。 |
| 宠物清单 | 展示档案数、需关注护理、待办护理、体重记录数和永久历史；周期护理可在保存后计算下次安排。 |
| 推荐清单 | 展示全部、想去、去过、五星和旅行记录；星级、标签、再次推荐原因与地点状态可组合筛选。 |
| 家庭投票 | 展示进行中投票、总投票数、临近截止、模板和新建入口；结果随投票实时刷新，并保留可分享链接。 |

### 视觉、动效与可访问性规范

1. 五个模块均使用同一套暖白底色、深棕文字、橙红主操作色和绿色成功状态，避免页面间色调跳变。
2. 状态透镜采用横向滚动但不遮挡正文；390px 窄屏下按钮保留可触达尺寸，内容不会横向溢出。
3. 快捷操作提供默认、悬停、按下、键盘焦点和禁用状态，焦点环保持清晰可见。
4. 筛选、保存、切换等反馈使用 180-260ms 的透明度和位移动画；开启 `prefers-reduced-motion` 时自动收敛为即时反馈。
5. 列表、卡片和图片均保留稳定尺寸与加载占位，减少图片解码或异步刷新造成的布局跳动。
6. 触发状态变化后用文本提示而非仅依赖颜色；重要操作继续保留可读标签和语义化按钮。
7. 模块入口与快捷操作分层显示，主页不再放家庭时间线；时间线仅在推荐清单中以横线条形式展示。
8. 菜单四步导航只在“猫家点菜”流程可见，记事本、宠物、推荐和投票页面不会误显示 1-4 步。
9. 本地图片继续使用固定比例、`alt` 文本和失败回退，保证弱网及移动端滚动的稳定性。
10. 所有关键操作遵循“一个屏幕内可开始”的原则，避免把日常记录变成复杂配置流程。

### 验证记录

1. 已执行 `node --check public/app.js`、`node --check server.js` 与 `git diff --check`，均通过。
2. 已使用 390px 移动端尺寸检查点菜、记事本、宠物、推荐、投票五个入口及状态透镜，无横向溢出或控制台错误。
3. 已检查推荐清单的横线时间线仅在推荐模块出现；四步导航只在点菜流程出现。
4. 已确认 `FamilyMenuServer` 仍在 3001 端口提供服务，cpolar 公网入口返回 HTTP 200。
5. 本轮相关代码提交为 `074096b Add daily quick workflows across family modules` 与 `a15d080 Add actionable status lenses across family modules`；本次文档更新单独提交，未纳入本地验收截图。

## 回归标准

1. `node --check server.js` 与 `node --check public/app.js` 均必须通过。
2. 新增接口需验证：记事本周览、固定状态、宠物记录、推荐和打卡均返回正常 JSON。
3. 390px 宽度下不得横向溢出；四个入口分别可打开、可返回主页。
4. 所有图片必须为本地受控路径并有 `alt` 文本；动效遵从 `prefers-reduced-motion`。
