# 猫家日常安全与优化审计

审计日期：2026-08-21

## 结论

本轮已完成通知、上传、日期、投票、SQLite、前端注入、缓存、URL 状态、触控尺寸和主要 N+1 查询的修复。生产依赖审计为 0 个已知漏洞，自动化测试 10/10 通过，真实数据库完整性检查正常。

项目仍按家庭约束保持“公网免登录、管理功能可操作”。因此知道 cpolar 地址的人仍可读取家庭内容、伪装成员并修改或删除数据。限流、掩码、冷却和输入校验只能降低滥用概率与影响，不能替代身份认证。

## 已完成修复

| 范围 | 实现 |
|---|---|
| 通知秘密 | `/api/notify/config` 与 `/api/notify/targets` 仅返回掩码和配置状态，秘密字段不回传。 |
| 通知费用 | 短信测试、配置更新和外部通知分别限流，同一餐次通知有 10 分钟冷却。 |
| SMTP/SSRF | 解析 SMTP 主机并拒绝环回、私网、链路本地和云元数据地址；邮件仅发送纯文本。 |
| 配置可靠性 | 字段白名单、原子写入，修改 SMTP 后清理缓存客户端。 |
| 图片上传 | Sharp 真实解码、WebP 重编码、元数据剥离、体积/像素/尺寸/目录配额限制。 |
| 孤儿文件 | 失败请求立即清理，未关联宠物附件超过 24 小时自动清理。 |
| 日期与枚举 | 严格验证真实日期、时间、餐次、资源编号和业务枚举。 |
| 普通投票 | 关闭投票时校验提交的发起人与原发起人一致。 |
| 匿名投票 | 服务端签发 HMAC HttpOnly Cookie，不再信任 `localStorage` 或客户端 `voter_key`。 |
| HTML 注入 | 菜品名称进入餐次面板前统一转义；继续保留 Helmet CSP 与参数化 SQL。 |
| 数据库 | 版本化迁移、启动备份、外键检查、WAL、忙等待、保留清理和优雅关闭。 |
| 查询性能 | 批量读取记事本 mentions/read、投票选项和首页提醒，移除主要 N+1。 |
| 发布缓存 | HTML/JS/CSS 使用协商校验并加资源版本号；图片等稳定资源长期缓存。 |

## 运维命令

```bash
npm run check
npm run db:check
npm run db:backup
npm audit --omit=dev
```

默认启动备份目录为 `data/backups/`，保留最近 10 份。也可通过环境变量调整运行位置：

```text
FAMILY_DB_PATH
FAMILY_BACKUP_DIR
FAMILY_NOTIFY_CONFIG_PATH
FAMILY_COOKIE_SECRET_PATH
FAMILY_SECURE_COOKIES=1
```

公网 HTTPS 经 cpolar 代理时建议设置 `FAMILY_SECURE_COOKIES=1`，使匿名投票 Cookie 仅通过 HTTPS 发送。

## 验证结果

| 检查 | 结果 |
|---|---|
| Node 语法与测试 | 10/10 通过 |
| SQLite integrity / foreign keys | 通过 / 0 个错误 |
| 生产依赖漏洞 | 0 |
| 伪图片、超限图片、无效日期 | 已拒绝 |
| 通知秘密回传 | 未发现 |
| 390 / 768 / 1440px 横向溢出 | 未发现 |
| 推荐 URL、刷新与返回恢复 | 通过 |
| 减弱动态效果 | CSS 与 JS 均遵循系统偏好 |

## 剩余风险

1. **公网免登录**：URL 持有者可读取和修改家庭数据，这是当前最高且无法技术消除的风险。
2. **身份可伪装**：需要身份语义的旧接口仍以家庭成员编号作为操作人；没有登录就无法证明真实身份。
3. **限流边界**：公共操作限流可防误触和低频滥用，不能抵御分布式攻击或不断更换 cpolar 域名的攻击者。
4. **单机存储**：SQLite 和上传文件依赖当前电脑磁盘；应把备份定期复制到另一块受信任存储。
5. **大型单文件**：`server.js`、`public/app.js`、`public/style.css` 仍较大，后续宜按领域逐步拆分，但不应在一次发布中做高风险重写。

## 公网使用建议

1. cpolar 地址只发给家人，不在群聊、公开仓库或截图中长期暴露。
2. 不使用时关闭 cpolar；发生异常修改时立即更换域名并从最近备份恢复。
3. 阿里云 AccessKey 使用独立 RAM 子账号，仅授予短信发送所需权限，并设置费用告警。
4. 每次发布前运行四条运维命令；每周额外复制一份数据库备份到离线位置。
5. 若未来接受最小身份验证，家庭 PIN 或一次性邀请链接将是降低剩余风险最有效的下一步。
